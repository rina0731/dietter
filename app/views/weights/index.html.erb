<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <head>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/2.0.2/jquery.min.js"></script>
  </head>
</head>

<body class="home">
  <div class="img">
  <h1 class="effect"><small>体重変化：グラフ</small>
    <% time = Time.now # Thu Dec 24 00:00:00 +0900 2011  %>
    <%= time.strftime("%Y/%m/%d(#{%w(日 月 火 水 木 金 土)[Time.now.wday]})") %><br>
      体重&nbsp;<%= @user.latest_weight %> kg<br>
      BMI&nbsp;<%= (@user.latest_weight / ( @user.height / 100.0 ) ** 2).round(2) %></small></h1>
  </div>
  <main>
    <h4 class="dark">現在までの体重変化 </h4><br>
    <%= high_chart("sample", @graph) %>
  </main>
</body>
</html>